<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Rendering from template engine!!!</h1>
    <h3>Contacts List:</h3>
    <ul>
      <% contacts_list.forEach((contact) => { %>
      <li>
        <strong><%= contact.name %>: </strong><span><%= contact.number %></span>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
